server:
  port: 5555
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db
    username: postgres
    password: skibidi
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: rere.ddd1426@gmail.com
    password: tjvbukozzhlcfeyg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


token:
  access:
    expiryMs: 86400000 # 24 hours
